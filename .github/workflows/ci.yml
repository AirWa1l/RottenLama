name: CI/CD Pipeline for React with Vite

on:
  push:
    branches:
      - main
      - pre-production
      - develop
      - feature/*
  pull_request:
    branches:
      - main
      - pre-production
      - develop

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Check out the code
      - name: Checkout code
        uses: actions/checkout@v3

      # 2. Setup Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'  # Reemplaza con tu versión si usas otra

      # 3. Install dependencies
      - name: Install dependencies
        run: npm install

      # 4. Run tests
      - name: Run tests
        run: npm test
        continue-on-error: true  # Evita que falle el pipeline si no hay pruebas

      # 5. Build the project
      - name: Build project
        run: npm run build

      # 6. Upload build artifacts
      - name: Upload build artifacts
        uses: actions/upload-artifact@v3
        with:
          name: build
          path: dist

      # 7. Deploy to Production
      - name: Deploy to Production
        if: github.ref == 'refs/heads/main'
        run: echo "🚀 Desplegando a producción..."

      # 8. Deploy to Staging
      - name: Deploy to Staging
        if: github.ref == 'refs/heads/pre-production'
        run: echo "🚀 Desplegando a pre-producción..."
